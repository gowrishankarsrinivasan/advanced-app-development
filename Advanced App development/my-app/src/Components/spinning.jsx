import React from "react";
import "./Spinning.css"; // Import CSS for styling the spinner

const LoadingSpinner = () => {
  return (
    <div className="ring">
      
    </div>
  );
};

export default LoadingSpinner;
